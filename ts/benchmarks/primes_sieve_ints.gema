func [T] fill(x: T, n: Int) {
  map(\_ x, 0i..n-1i) | collect
}

func sieve(upto: Int) {
  isPrime = fill(true, upto) | unsafeTrans;
  primes = []:Int | trans;
  for i = range(2i, upto) {
    if isPrime(i-1i) | unwrap {
      push(i, primes);
      if i * i < upto {
        for m = range(0i, (upto - i*i) / i) {
          put(false, m*i + i*i - 1i, isPrime);
        }
      }
    };
  }
  primes | detrans
}

100000i | sieve | last
