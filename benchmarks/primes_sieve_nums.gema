func [T] fill(x: T, n: Num) {
  map(\_ x, 0..n-1) | collect
}

func sieve(upto: Num) {
  isPrime = fill(true, upto) | unsafeTrans;
  primes = []:Num | trans;
  for i = range(2, upto) {
    if isPrime(i-1) | unwrap {
      push(i, primes);
      if i * i < upto {
        for m = range(0, (upto - i*i) / i) {
          put(false, m*i + i*i - 1, isPrime);
        }
      }
    };
  }
  primes | detrans
}

100000 | sieve | last
